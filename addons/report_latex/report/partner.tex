\documentclass{report}
${helper.set_language()}

\title{Resumen de Clientes}
\author{${user.name}}
\logo{${helper.putImage(company.logo, scale=0.3)}}
\name{${company.name}}

\begin{document}

\maketitle

<% accum = 0 %>

% for par in objects:  
\section{${par.name | tex}}

<% accum += 1%>

\begin{itemize}
%	if par.document_type_id:
	\item ${par.document_type_id.name | tex }: ${par.document_number}
%	endif
%	if par.street:
	\item ${par.street | tex }
%	endif
%	if par.street2:
	\item ${par.street2 | tex }
%	endif
	\item Debe: ${par.debit}
\end{itemize}

\fancyfoot[C]{No de clientes ${accum}}

% endfor

\end{document}

